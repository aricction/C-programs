#include<stdio.h>
int main()
{
    char str[10],i,len;
    printf("enter any string \n");
    scanf("%d",&str[i]);

    for(i=0;i<str[i];i++)
    if(str[i]!='\0')
    {
        len++;
    }
    printf("length of string %d",len);
}
